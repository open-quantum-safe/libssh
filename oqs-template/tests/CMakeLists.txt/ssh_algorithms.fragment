
        set(PQ_SSH_ALGORITHMS 
{%- for sig in config['sigs'] %}
    {%- for variant in sig['variants'] %}
            ssh-{{ variant['name']|lower|replace('_','') }}
        {%- for classical_alg in variant['mix_with'] %}
            ssh-{{ classical_alg.name }}-{{ variant['name']|lower|replace('_','') }}
        {%- endfor -%}
    {%- endfor %}
{%- endfor %}
            )


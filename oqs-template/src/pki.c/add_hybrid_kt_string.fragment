
{%- for sig in config['sigs'] %}
    {%- for variant in sig['variants'] %}
        {%- for classical_alg in variant['mix_with'] %}
    if (strcmp(name, "ssh-{{ classical_alg.name }}-{{ variant['name']|lower|replace('_','') }}") == 0) {
        return SSH_KEYTYPE_{{ classical_alg.name|upper }}_{{ variant['name'] }};
    }
        {%- endfor -%}
    {%- endfor %}
{%- endfor %}


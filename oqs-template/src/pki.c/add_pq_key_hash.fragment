
{%- for sig in config['sigs'] -%}
    {%- for variant in sig['variants'] %}
    if (strcmp(name, "ssh-{{ variant['name']|lower|replace('_','') }}") == 0) {
        return SSH_DIGEST_AUTO;
    }
    {%- endfor %}
{%- endfor %}


{%- for sig in config['sigs'] %}
    {%- for variant in sig['variants'] %}
    case SSH_KEYTYPE_{{ variant['name'] }}:
      return "ssh-{{ variant['name']|lower|replace('_','') }}";
    {%- endfor %}
{%- endfor %}


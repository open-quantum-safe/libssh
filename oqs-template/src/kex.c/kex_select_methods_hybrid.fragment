
{%- for kem in config['kems'] %}
    {%- for variant in kem['variants'] %}
    } else if (strcmp(session->next_crypto->kex_methods[SSH_KEX], KEX_ECDH_NISTP384_{{ variant['name'] }}_SHA384) == 0){
        session->next_crypto->kex_type=SSH_KEX_ECDH_NISTP384_{{ variant['name'] }}_SHA384;
    {%- endfor %}
{%- endfor %}

